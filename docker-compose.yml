version: '3.8'

services:
  welding-app:
    build: .
    container_name: welding-defect-app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
